    用commit -a以当前目录下内容作为最终提交，最终版本的内容以当前文件夹当中的内容固定
    只以当前文件夹中内容提交，不对以前改名和删除的文件再做跟踪，用git ls-files查看文件内容时
    跟踪改名和删除的文件已经不存在了，在提交页面和提交后的提示当中均显示了跟踪被去掉了的状态。


修改分支名称 git branch -m oldbranch newbranch
用man git-branch命令查看即得到帮助

iftop -i eth0		//查看网卡的流量

