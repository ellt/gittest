1. 常用函数：
    1.1 打印：
        dump($_SERVER)
        echo "<pre>"
        print_r($_SERVER)
    1.2 数组：
        array_merge(ubclude './Conf/config.php', $config);
    1.3 加载：
        load('@.function'); 加载当前目录下 fuciotn文件

2. 配置相关：
    "LOAD_EXT_FILE" ==> "function", 配置自动加载文件的名称
    "URL_HTML_SUFFIX" ==> ""  伪静态后缀名
    "TMPL_PARSE_STRING" ==>>array( '__PUBLIC__'=>__ROOT__.'/'.APP_NAME.'TPL/Public' ), 模板相关配置
    "TMPL_TEMPLATE_SUFFIX" ==> '.htm' //模板后自动加载的后缀名配置项

3. 常量：
    IS_POST ==> 判断是否表单提交